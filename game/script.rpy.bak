# 游戏的脚本可置于此文件中。

# 声明此游戏使用的角色。颜色参数可使角色姓名着色。

# 游戏在此开始。

label start:

    # call scene1
    call scene2
    # call scene3
    # call scene4
    # call scene5
    call scene6
    # call scene7

    # 此处为游戏结尾。

    return
